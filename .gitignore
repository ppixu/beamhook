*.xcodeproj
.build/
build/
DerivedData/
.landingpage-temp/
# wrangler drops a local cache here when sign-release.sh uploads the update zip
.wrangler/

# Local planning notes — never published
MONETIZATION.md
MONETIZATION.html
NOTARIZING.html
docs/design.md
docs/implementation-plan.md
.DS_Store

# Claude Code guidance — kept local, not tracked
CLAUDE.md

# Local release copy/launch notes
launch-copy.md

# Built distribution artifacts — the paid binary must never land in the public
# repo; they go to Gumroad and the R2 update host instead (see RELEASING.md).
Beamhook-*.zip
Beamhook-*.dmg
# A built .app can also land in the repo root as a signing/staging leftover.
/Beamhook.app/

# Release tooling stays out of the public repo. These all still live here on
# disk and are still how a release is cut; copies are kept in
# ~/Dropbox/Apps/Beamhook/release-scripts/ so they survive a fresh clone.
# Note they were public until 1.1.5, so they remain readable in git history.
/ship.sh
/release.sh
/RELEASING.md
/scripts/sign-release.sh
